# 快速启动指南

## ✅ 数据库配置已完成

数据库信息：
- 主机: localhost
- 端口: 3306
- 数据库: supermarket_db
- 用户: root
- 密码: wuhai1

已创建的表：
- users (用户表)
- categories (商品分类表)
- products (商品表)
- inventory_logs (库存变动记录表)
- sales_orders (销售订单表)
- sale_items (销售订单明细表)
- purchase_orders (采购入库单表)
- purchase_items (采购明细表)
- operation_logs (操作日志表)
- stock_alerts (库存预警表)

默认管理员账户：
- 用户名: admin
- 密码: admin123

⚠️ 重要提示：首次登录后请立即修改默认密码！

## 🚀 启动项目

### 方法一：分别启动（推荐用于开发）

#### 1. 安装依赖

```bash
# 安装后端依赖
npm install

# 安装前端依赖
cd client
npm install
cd ..
```

#### 2. 启动后端服务

打开第一个终端窗口，运行：

```bash
npm run dev
```

后端服务将在 `http://localhost:5000` 启动

#### 3. 启动前端服务

打开第二个终端窗口，运行：

```bash
cd client
npm start
```

前端服务将在 `http://localhost:3000` 启动，浏览器会自动打开

### 方法二：使用 PM2（推荐用于生产）

#### 1. 安装 PM2

```bash
npm install -g pm2
```

#### 2. 启动后端

```bash
pm2 start server.js --name supermarket-api
```

#### 3. 启动前端

```bash
cd client
npm run build
pm2 serve build --name supermarket-client --spa
```

#### 4. 查看状态

```bash
pm2 status
```

## 📱 访问系统

1. 打开浏览器访问：http://localhost:3000

2. 使用默认管理员账户登录：
   - 用户名: admin
   - 密码: admin123

3. 登录成功后，建议立即修改密码

## 🎯 功能测试

### 1. 测试商品管理
- 点击左侧菜单"商品管理"
- 点击"添加商品"创建测试商品
- 填写商品信息并保存

### 2. 测试销售功能
- 点击左侧菜单"销售管理"
- 点击"新建销售"
- 搜索商品添加到购物车
- 完成销售订单

### 3. 测试库存管理
- 点击左侧菜单"库存管理"
- 查看商品库存状态
- 测试库存调整功能

### 4. 测试用户管理（仅管理员）
- 点击左侧菜单"用户管理"
- 创建新用户
- 分配角色权限

## 🔧 常见问题

### 问题1：端口被占用

**错误信息**: `Error: listen EADDRINUSE: address already in use :::5000`

**解决方案**：
1. 修改 `.env` 文件中的 `PORT` 为其他端口（如 5001）
2. 或者停止占用该端口的程序

### 问题2：数据库连接失败

**错误信息**: `数据库连接失败`

**解决方案**：
1. 确认 MySQL 服务已启动
2. 检查 `.env` 文件中的数据库配置
3. 确认数据库用户名和密码正确

### 问题3：前端无法访问后端API

**解决方案**：
1. 确认后端服务已启动
2. 检查浏览器控制台错误信息
3. 确认前后端端口配置正确

### 问题4：npm install 失败

**解决方案**：
1. 检查网络连接
2. 尝试使用淘宝镜像：
```bash
npm config set registry https://registry.npmmirror.com
```

## 📊 数据库管理

### 查看数据库

```bash
# 连接到MySQL
mysql -u root -pwuhai1

# 使用数据库
USE supermarket_db;

# 查看所有表
SHOW TABLES;

# 查看用户
SELECT * FROM users;

# 查看商品
SELECT * FROM products;

# 退出
EXIT;
```

### 备份数据库

```bash
mysqldump -u root -pwuhai1 supermarket_db > backup_$(date +%Y%m%d).sql
```

### 恢复数据库

```bash
mysql -u root -pwuhai1 supermarket_db < backup_20240101.sql
```

## 🎨 界面特点

本系统专为老人操作设计：

- **大字体**：16-20px 字体大小，易于阅读
- **大按钮**：44-56px 高度的按钮，易于点击
- **高对比度**：清晰的色彩对比，视觉识别度高
- **简洁布局**：功能分区明确，操作流程简单
- **直观图标**：使用清晰的图标辅助理解

## 🔐 安全建议

1. **修改默认密码**
   - 首次登录后立即修改 admin 用户的密码
   - 修改 `.env` 中的 JWT_SECRET

2. **定期备份数据**
   - 建议每天备份数据库
   - 保留最近7天的备份

3. **权限管理**
   - 根据员工职责分配适当的角色
   - 定期审查用户权限

4. **操作审计**
   - 定期查看操作日志
   - 发现异常操作及时处理

## 📞 技术支持

如遇到问题，请：
1. 查看本文档的"常见问题"部分
2. 检查控制台错误日志
3. 联系技术支持团队

---

祝您使用愉快！